# Required: your DeepSeek API key.
DEEPSEEK_API_KEY=your-api-key

# Web UI port on the host and in the container.
PORT=3080

# Host folder mounted into /home/node. Relative paths start from the compose directory.
WORK_PATH=../work

# Image build versions.
NODE_VERSION=22
DSH_VERSION=latest
